from flask_socketio import emit
from app import socketio

# @socketio.on('connect')
# def test_connect():
#     emit('status', {'data': 'Initializing', 'balance': 'Initializing', 'cost': 'Initializing'})
